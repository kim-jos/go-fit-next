export {};
// import { supabaseClient } from "../utils/database/supabase.key";

// export async function getRatings(): Promise<ClassRatings[]> {
//   let { data, error } = await supabaseClient
//     .from(classRatingsTable)
//     .select("*");

//   if (error) {
//     throw new Error(`GET / ${classRatingsTable} error: ${error.message}`);
//   }

//   return data;
// }
